# This file is a template, and might need editing before it works on your project.
# Official docker image.

variables:
  BOOST_ROOT: "$CI_PROJECT_DIR\\libs\\boost_1_64_0"
  GIT_SUBMODULE_STRATEGY: "normal"
  IS_FXSERVER: "1"

stages:
  - prep
  - build
  - test
  - deploy

prep-linux:
  artifacts:
    paths:
    - fivem/code/bin
    untracked: true
  image: alpine:latest
  stage: prep
  variables:
      GIT_STRATEGY: clone
  script:
    - git clone https://github.com/citizenfx/fivem.git
    - cd fivem
    - git submodule update --init --recursive
    - ln -sf $CI_PROJECT_DIR/components/fs-web $CI_PROJECT_DIR/fivem/code/components/fs-web
    - echo "component 'fs-web'" >> $CI_PROJECT_DIR/fivem/code/components/config.lua
    - cd ..
    - chmod +x $CI_PROJECT_DIR/Build-Scripts/scripts/fxs-components/proot-build.sh
    - $CI_PROJECT_DIR/Build-Scripts/scripts/fxs-components/proot-build.sh
  tags:
  - docker
build:
  stage: prep
  tags:
    - win32
  cache:
    paths:
      - libs\boost_1_64_0
      - save\y
  before_script:
    - git clone https://github.com/citizenfx/fivem.git
    #- powershell -File $CI_PROJECT_DIR\Build-Scripts\scripts\fxs-components\Installer.ps1 -mode premake
    - .$env:CI_PROJECT_DIR\Build-Scripts\scripts\fxs-components\Installer.ps1 -mode boost
    - cd fivem
    #- git submodule update --init --recursive
  script:
    - ."$env:CI_PROJECT_DIR\Build-Scripts\scripts\fxs-components\FiveM.ps1" -WorkDir "$env:CI_PROJECT_DIR\fivem" -SaveDir "$env:CI_PROJECT_DIR\save"
  artifacts:
    paths:
    - save\bin